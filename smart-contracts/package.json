{
  "name": "event-ticket-smart-contracts",
  "version": "1.0.0",
  "type": "commonjs",
  "description": "Smart contracts for Event Ticket NFT on Lisk",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run contract-scripts/deploy.js --network liskTestnet",
    "deploy:mainnet": "hardhat run contract-scripts/deploy.js --network liskMainnet",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^17.2.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "ethers": "^6.13.4"
  }
}
